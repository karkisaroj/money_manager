import 'package:flutter/material.dart';

class IncomeAddition extends StatelessWidget {
  const IncomeAddition({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
