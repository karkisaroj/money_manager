import 'package:flutter/material.dart';

class MoneyAddWidget extends StatelessWidget {
  const MoneyAddWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
